// 自动生成于 {{ sourceFile }} 时间: {{ generatedAt }}
// 请勿手动编辑此文件
{%- for enum in enums %}

export enum {{ enum.name }} {
{%- for item in enum.values %}
  /** {{ item.name }} */
  {{ item.name }} = {{ item.value }},
{%- endfor %}
}
{%- endfor %}
