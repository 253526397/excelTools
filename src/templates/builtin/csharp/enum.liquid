// 自动生成于 {{ sourceFile }} 时间: {{ generatedAt }}
// 请勿手动编辑此文件
{%- if namespace %}

namespace {{ namespace }}
{
{%- endif %}
    /// <summary>
    /// {{ enumName }} 枚举
    /// </summary>
    public enum {{ enumName }}
    {
{%- for item in values %}
        /// <summary>{{ item.name }}</summary>
        {{ item.name }} = {{ item.value }},
{%- endfor %}
    }
{%- if namespace %}
}
{%- endif %}
